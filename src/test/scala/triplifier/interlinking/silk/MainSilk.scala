package triplifier.interlinking.silk

/**
 * CHECK: how to wrap silk in the most simple way
 * CHECK: how to extend silk mapping for record linkage on two different tables from JDBC
 */
object MainSilk extends App {

  //  val exampleDir = new File("")
  //  val linkSpecFile = new File("exampleDir", "linkSpec.xml")
  //  val outputFile = new File(exampleDir, "links.nt")
  //
  //  Silk.executeFile(linkSpecFile)
  //  outputFile.exists()

  //  Silk.executeFile(linkSpecFile, "linkSpecID", 4, true)

}